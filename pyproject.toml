[project]
name = "particle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "songyang", email = "1460854340@qq.com" }
]
requires-python = ">=3.14"
dependencies = [
    "h5py>=3.16.0",
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "scikit-learn>=1.9.0",
    "scipy>=1.18.1",
    "torch>=2.13.0",
]

[project.scripts]
particle = "particle:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
